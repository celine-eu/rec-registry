apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: rec-registry

build:
  artifacts:
    - image: ghcr.io/celine-eu/rec-registry
      docker:
        dockerfile: Dockerfile
  local:
    push: false
    useDockerCLI: true
  tagPolicy:
    sha256: {}
